[project]
name = "test"
version = "0.1.0"
description = "Test project"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 4 - Beta"
]

[tool.setuptools]
zip-safe = "false"

[tool.setuptools.package-data]
"*" = ["py.typed"]