[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name="pyvelocity"
version="0.1.0"
description = "Test for invalid requires-python"
readme = "README.md"
requires-python = "invalid-version"
classifiers = [
    "Programming Language :: Python :: 3.9"
]

[tool.setuptools]
zip-safe = false